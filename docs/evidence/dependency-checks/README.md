# Dependency-check evidence

Saved responses and screenshots backing the dependency-verification table
in `docs/scoping-decision.md` — one file (or a short folder) per dependency
you actually exercised this week: the `curl` output, the downloaded
sample, the signup confirmation, whatever proves you didn't just read the
docs page.

Name files so they're traceable to the table row, e.g.
`2026-09-05-product-lookup-api-curl.txt` or
`2026-09-05-recipe-dataset-download/`. Anything you couldn't exercise gets
marked **UNVERIFIED** in the table instead of a file here.

This file is a placeholder so the empty folder survives `git add` — delete
it once real evidence lands here.
